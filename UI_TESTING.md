# اختبارات واجهة المستخدم المتجاوبة

أضيفت اختبارات Playwright إلى المشروع للتحقق تلقائيًا من واجهة Sehaty على ثلاثة ملفات تعريف: هاتف بعرض iPhone 13، وتابلت بعرض iPad Mini، وسطح مكتب بعرض Desktop Chrome. تستخدم الملفات التعريفية Chromium مع مقاسات الأجهزة حتى تعمل بصورة مستقرة في بيئات CI التي لا تحتوي مكتبات WebKit.

## التشغيل

```bash
npm run test:ui
```

ويشغّل الأمر التالي مجموعة الاختبارات الكاملة، بما في ذلك lint وbuild واختبارات Flask واختبارات UI:

```bash
npm test
```

للمراجعة التفاعلية يمكن تشغيل الاختبارات في وضع المتصفح المرئي:

```bash
npm run test:ui:headed
```

## التغطية الحالية

تتحقق الاختبارات من عدم وجود تجاوز أفقي عند عروض 375 و768 و1024 و1440 بكسل. كما تتحقق من ظهور شريط التنقل العام على سطح المكتب، وتحول التابلت والهاتف إلى قائمة قابلة للفتح والإغلاق، وسلامة حقول نموذج التواصل على شاشة هاتف ضيقة، وعمل زر الدعوة الأساسي الذي ينقل المستخدم إلى التسجيل.

تُحفظ اللقطات والفيديو وtrace عند الفشل فقط، بينما يتم تجاهل مجلدي `playwright-report/` و`test-results/` في Git حتى لا تتضخم المستودعات. عند إضافة مكون بصري جديد أو تغيير breakpoint، يجب تشغيل `npm run test:ui` قبل الدمج.

## اختبارات الأداء

تُشغّل اختبارات الأداء بالأمر التالي:

```bash
npm run test:performance
```

تقيس هذه الاختبارات زمن استجابة الخادم، ووقت `DOMContentLoaded`، ووقت حدث `load`، ووقت ظهور أول محتوى مرئي `FCP`، وحجم JavaScript وCSS والموارد المشفرة، إضافة إلى زمن استجابة فتح قائمة الهاتف. كما تتحقق من عدم فشل موارد المستند الأساسية في الصفحة العامة.

العتبات الحالية هي استجابة أولية أقل من 1500ms، وDOMContentLoaded أقل من 3500ms، وload أقل من 5000ms، وFCP أقل من 3000ms، وحجم JavaScript المضغوط أقل من 1.9MB، وحجم CSS أقل من 400KB، وإجمالي الموارد المشفرة أقل من 2.5MB. هذه العتبات هي بوابة إنذار مبكر وليست بديلًا عن قياس Production عبر مستخدمين حقيقيين.

يُفضّل تشغيل `npm run test:performance` على نسخة build/preview عند اعتماد تغييرات كبيرة في الحزم، لأن خادم Vite التطويري لا يمثل ضغط الشبكة وCDN في الإنتاج بصورة كاملة.
