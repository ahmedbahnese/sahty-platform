# Sehaty Implementation Baseline

**تاريخ التدقيق:** 2026-08-18

هذا الملف يصف الحالة الفعلية التي تم التحقق منها قبل بدء دورة التنفيذ الشاملة. لا يساوي وجود route أو model بأن الميزة مكتملة في واجهة الويب أو Flutter.

## الحالة العامة

المشروع يحتوي على واجهة React/Vite، وBackend مبنيًا على Flask وSQLAlchemy وFlask-Migrate، ومجلد Flutter أساسه موجود في `mobile/`. المستودع يعمل حاليًا بفرع `main` متزامن مع GitHub، وتوجد اختبارات Backend متعددة واختبارات Flutter أساسية، لكن تشغيل Flutter الفعلي يحتاج Flutter SDK غير المتاح في بيئة التدقيق الحالية.

## Backend وWeb

توجد Blueprints فعلية للمصادقة، المستخدمين، الإدارة، المواعيد، الوصفات، الأدوية، المختبر والأشعة، الطوارئ، السجل الطبي، الأسرة، الإشعارات، بنك الدم، الطبيب، التمريض، المستشفيات، الصيدليات، التطعيمات، والدليل الصحي. توجد models مقابلة لمعظم هذه المجالات.

تتطلب تهيئة Flask متغير `SESSION_SECRET` أو `JWT_SECRET` قبل الاستيراد، ولذلك فشل أمر `flask --app main db heads` عند تشغيله دون Secret. هذا سلوك أمني صحيح وليس دليلًا على عطل في التطبيق. يجب تشغيل أوامر Flask مع Secret موجود في البيئة، دون تخزينه في Git.

## Mobile

يحتوي Flutter foundation على API client وsecure storage وإدارة جلسة وتوجيه وtheme وmodels واختبارات لـAPI client والجلسة والمستخدم. وفق الجرد السابق، المصادقة الأساسية مرتبطة بالمسارات الحقيقية، بينما معظم workflows الطبية التفصيلية تحتاج تنفيذ واجهات وربطًا واختبارات إضافية.

## Replit وProduction

توجد إعدادات `replit.nix` و`replit.md` وسكربتات تشغيل وفحص. سكربت API يفرض وضع الإنتاج افتراضيًا، يطلب `SESSION_SECRET`، ويشغّل build ثم migrations ثم Gunicorn على `0.0.0.0:$PORT`. وضع الإنتاج يرفض SQLite ويتطلب PostgreSQL عبر `DATABASE_URL`.

لا توجد في بيئة التدقيق الحالية قاعدة PostgreSQL إنتاجية موصولة أو Secrets إنتاجية متاحة للاختبار. لذلك يجب تصنيف أي تحقق للإنتاج كـ`NOT VERIFIED` إلى أن تُضبط Replit Secrets وتُختبر قاعدة PostgreSQL الفعلية بأمان.

## الأولويات التنفيذية

الأولوية الأولى هي تثبيت مسار الإقلاع والـmigrations والـhealth/readiness بقاعدة اختبار نظيفة. بعد ذلك تُراجع المصادقة وملكية الموارد، ثم تُنفذ workflows التي تدعمها النماذج والـAPI فعليًا، ثم تُربط واجهة الويب وFlutter دون بيانات وهمية. Android وiOS لا يُصنفان جاهزين إلا بعد نجاح build فعلي في بيئتيهما.

## معيار عدم الادعاء

لا تُصنف ميزة على أنها `WORKING` إلا إذا كانت مرتبطة ببيانات حقيقية، endpoint فعلي، اختبار قابل للتكرار، وحالة loading/empty/error مناسبة. أي ميزة لا تحقق ذلك تصنف `PARTIAL` أو `NOT VERIFIED` أو `NOT IMPLEMENTED`.
